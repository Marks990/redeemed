name: Hello World

on:
  push:
    branches:
      - dock

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: |
          ls -larth;
          pwd;
          echo "👋 Hello, world! This GitHub Action ran on a push to the main branch."
          cd terra
          ls -larth
          cat Dockerfile
